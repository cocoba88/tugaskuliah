#!/usr/bin/env python3eimport ose3[yo)?ree3[yo)?syse3[yo)?timee3[yo)?shutile3[yo)?zipfilee3[yo)?tempfilee3[yo)?hashlibe3[yo)?itertoolse3[yo)?threadinge3[yo)?atexiterom pathlib import Patherom datetime import datetimeeAPP_VERSION = "v1.14.13"l.:GET_LIB = "libapp.so"j:CH = "ARM64-v8a"j::/OR = "PhoenixMods a.k.a 0xdeadc0de"kb:0UT_DIR = f"Dramora_{APP_VERSION}"eR = "\033[1;31m"k = "\033[1;32m"l]= "\033[1;33m"j?= "\033[1;34m"jI= "\033[1;36m"kI= "\033[1;35m"lI= "\033[1;37m"lS= "\033[0m"eANIMATION_SPEED = {g   "typing": 0.008,g   "title": 0.008,g   "spinner": 0.12,g   "progress": 0.08,g   "patch_delay": 0.04,g   "step_delay": 0.5e3lATCHES = {g   "Enabled Premium (Force True)": {g       "patch": "C2820091",g       "offsets": [g           0x73D080, 0x465C88, 0x465C8C, 0x55B4D4, 0x55BEE4,g           0x55BFA8, 0x55BFAC, 0x69EA6C, 0x69EAB0, 0x69EAD8,g           0x69EB00, 0x69EB28, 0x73DF3C, 0x73DF54, 0x73E09C,g           0x73E0BC, 0x73E18C, 0x73E1AC, 0x73E2C0, 0x73E2E0,g           0x73E3B8, 0x73E3D8,g       ]g   },g   "Disabled Ads (Force False)": {g       "patch": "01000014",g       "offsets": [g           0x5727F8, 0x572CD4, 0x572E60, 0x574A00, 0x5750F8,g           0x5753F8, 0x692370, 0x692E3C, 0x693140, 0x693378,g           0x693478, 0x693570, 0x761FBC,g       ]g   }e3e=n[_dirs = []edef cleanup_temp_dirs():g   for temp_dir in temp_dirs:g       try:g           if os.path.exists(temp_dir):g               shutil.rmtree(temp_dir)g       except Exception:g           passeatexit.register(cleanup_temp_dirs)edef clear():g   os.system("cls" if os.name == "nt" else "clear")edef terminal_width():g   try:g       return shutil.get_terminal_size(fallback=(120, 30)).columnsg   except Exception:g       return 120edef strip_ansi(text):g   ansi_escape = re.compile(r'\x1B(?:[@-Z\\-_]|\[[0-?]*[ -/]*[@-~])')g   return ansi_escape.sub('', text)edef visible_length(text):g   return len(strip_ansi(text))edef center_text(text):g   width = terminal_width()g   visible = visible_length(text)g   padding = max((width - visible) // 2, 0)g   return (" " * padding) + textedef gradient_text(text):g   colors = ["\033[38;5;51m", "\033[38;5;45m", "\033[38;5;39m", "\033[38;5;33m", "\033[38;5;63m"]g   result = ""g   for i, char in enumerate(text):g       result += colors[i % len(colors)] + charg   return result + Xedef typing(text, speed=None, centered=True):g   speed = speed or ANIMATION_SPEED["typing"]g   if centered:g       text = center_text(text)g   for char in text:g       print(char, end="", flush=True)g       time.sleep(speed)g   print()edef animated_title(text, speed=None):g   speed = speed or ANIMATION_SPEED["title"]g   width = terminal_width()g   for i in range(len(text) + 1):g       raw_text = text[:i]g       current = gradient_text(raw_text)g       visible = len(raw_text)g       padding = max((width - visible) // 2, 0)g       sys.stdout.write("\r" + (" " * width))g       sys.stdout.write("\r" + (" " * padding) + current)g       sys.stdout.flush()g       time.sleep(speed)g   print("\n")edef cyberpunk_divider():g   line = f"{M}{'`]]* 6}{C}{'_4' * 58}{M}{'`]]* 6}{X}"g   print(center_text(line))edef glass_box(lines):g   visible_widths = [visible_length(x) for x in lines]g   width = max(visible_widths) + 6g   top = f"{C}_Q{'^H' * width}_R{X}"g   bottom = f"{C}_T{'^H' * width}_S{X}"g   print(center_text(top))g   for line in lines:g       visible = visible_length(line)g       padding = width - visible - 2g       content = f"{C}^J{X}  {line}{' ' * padding}{C}^J{X}"g       print(center_text(content))g   print(center_text(bottom))edef status_card(title, value, color=G):g   return f"{W}{title:<14}{X}: {color}{value}{X}"edef centered_status_card(title, value, color=G):g   text = f"{W}{title}{X}: {color}{value}{X}"g   return center_text(text)edef banner(package_path, output_dir):g   clear()g   cyberpunk_divider()g   print()g   animated_title("`H DRAMORA LIB PATCH `H")g   print(center_text(f"{Y}DRAMORA Advanced ARM64 Binary Patching Engine{X}"))g   print()g   cyberpunk_divider()g   print()g   g   status_lines = [g       centered_status_card("Version", APP_VERSION),g       centered_status_card("Target Lib", TARGET_LIB),g       centered_status_card("Architecture", ARCH),g       centered_status_card("Package", os.path.basename(package_path)),g       centered_status_card("Output Dir", output_dir),g       centered_status_card("Author", AUTHOR),g   ]g   g   for line in status_lines:g       print(line)g   g   print()g   cyberpunk_divider()g   print()espinner_running = Falseedef spinner(text):g   global spinner_runningg   spinner_running = Trueg   symbols = itertools.cycle(["f3", "fA", "fa", "f`", "g", "f\", "fN", "fO", "f/", "f7"])g   while spinner_running:g       sys.stdout.write(f"\r{center_text(f'{Y}[{next(symbols)}]{X} {text}')}    ")g       sys.stdout.flush()g       time.sleep(ANIMATION_SPEED["spinner"])edef start_spinner(text):g   thread = threading.Thread(target=spinner, args=(text,), daemon=True)g   thread.start()g   return threadedef stop_spinner():g   global spinner_runningg   spinner_running = Falseg   time.sleep(0.2)g   sys.stdout.write("\r" + (" " * terminal_width()) + "\r")g   sys.stdout.flush()edef hex_to_bytes(hex_string):g   return bytes.fromhex(hex_string)edef md5_file(path):g   md5 = hashlib.md5()g   with open(path, "rb") as file:g       while chunk := file.read(4096):g           md5.update(chunk)g   return md5.hexdigest()edef extract_package(package_path, extract_dir):g   try:g       with zipfile.ZipFile(package_path, "r") as zip_ref:g           zip_ref.extractall(extract_dir)g       return Trueg   except Exception as error:g       print(center_text(f"{R}[ERROR]{X} Extract failed"))g       print(center_text(str(error)))g       return Falseedef verify_arm64(lib_path):g   try:g       with open(lib_path, "rb") as file:g           elf = file.read(0x20)g       if elf[:4] != b"\x7fELF":g           return Falseg       return elf[4] == 2g   except Exception:g       return Falseedef find_arm64_lib(base_dir):g   arm64_keywords = ["arm64", "arm64_v8a", "arm64-v8a"]g   g   for root, _, files in os.walk(base_dir):g       for file in files:g           if file != TARGET_LIB:g               continueg           full = os.path.join(root, file)g           lower = full.lower()g           if any(k in lower for k in arm64_keywords):g               if verify_arm64(full):g                   return fullg   g   for root, _, files in os.walk(base_dir):g       for file in files:g           if file != TARGET_LIB:g               continueg           full = os.path.join(root, file)g           try:g               if verify_arm64(full):g                   return fullg           except Exception:g               passg   g   for root, _, files in os.walk(base_dir):g       for file in files:g           lower = file.lower()g           if not lower.endswith(".apk"):g               continueg           if not any(k in lower for k in arm64_keywords):g               continueg           split_apk = os.path.join(root, file)g           split_extract = os.path.join(root, f"split_extract_{abs(hash(file))}")g           os.makedirs(split_extract, exist_ok=True)g           try:g               with zipfile.ZipFile(split_apk, "r") as zip_ref:g                   zip_ref.extractall(split_extract)g           except Exception:g               continueg           for r, _, f in os.walk(split_extract):g               for x in f:g                   if x != TARGET_LIB:g                       continueg                   full = os.path.join(r, x)g                   try:g                       if verify_arm64(full):g                           return fullg                   except Exception:g                       passg   return Noneedef progress(current, total):g   percent = int((current / total) * 100)g   filled = int(percent / 4)g   bar = "`S+* filledg   empty = "`I+* (25 - filled)g   text = f"{C}[{bar}{empty}]{X} {G}{percent}%{X} {Y}({current}/{total}){X}"g   sys.stdout.write(f"\r{center_text(text)}")g   sys.stdout.flush()edef animate_step(message, duration=1.2):g   dots = ["   ", ".  ", ".. ", "..."]g   steps = 30g   for i in range(steps):g       idx = i % len(dots)g       text = f"{Y}e({X} {message}{dots[idx]}"g       sys.stdout.write(f"\r{center_text(text)}    ")g       sys.stdout.flush()g       time.sleep(duration / steps)g   sys.stdout.write("\r" + (" " * terminal_width()) + "\r")g   sys.stdout.flush()edef patch_offset(data, offset, patch_bytes):g   if offset >= len(data):g       return Falseg   old = data[offset:offset + len(patch_bytes)]g   if old == patch_bytes:g       return Falseg   data[offset:offset + len(patch_bytes)] = patch_bytesg   return Trueedef centered_print(text, color=""):g   if color:g       text = f"{color}{text}{X}"g   print(center_text(text))edef main():g   if len(sys.argv) < 2:g       script_name = os.path.basename(sys.argv[0])g       centered_print("\n[USAGE]", R)g       centered_print(f"python3 {script_name} file.apk")g       centered_print(f"python3 {script_name} file.apks")g       centered_print(f"python3 {script_name} file.xapk")g       centered_print(f"python3 {script_name} file.apkm\n")g       sys.exit(1)g   g   package_path = sys.argv[1]g   g   if not os.path.exists(package_path):g       centered_print("[ERROR] File not found", R)g       sys.exit(1)g   g   ext = Path(package_path).suffix.lower()g   supported = [".apk", ".apks", ".xapk", ".apkm"]g   g   if ext not in supported:g       centered_print("[ERROR] Unsupported format", R)g       sys.exit(1)g   g   os.makedirs(OUTPUT_DIR, exist_ok=True)g   banner(package_path, OUTPUT_DIR)g   g   temp_dir = tempfile.mkdtemp(prefix="dramora_")g   temp_dirs.append(temp_dir)g   g   animate_step("Extracting package", 1.5)g   start_spinner("Extracting files")g   success = extract_package(package_path, temp_dir)g   stop_spinner()g   g   if not success:g       sys.exit(1)g   g   centered_print("[OK] Package extracted", G)g   g   animate_step("Searching ARM64 library", 1.2)g   start_spinner("Scanning directories")g   lib_path = find_arm64_lib(temp_dir)g   stop_spinner()g   g   if not lib_path:g       centered_print("[ERROR] ARM64 libapp.so not found", R)g       sys.exit(1)g   g   centered_print("[FOUND] ARM64 library detected", G)g   g   animate_step("Verifying ELF header", 1.0)g   g   if not verify_arm64(lib_path):g       centered_print("[ERROR] Invalid ARM64 ELF", R)g       sys.exit(1)g   g   centered_print("[OK] ARM64 ELF verified", G)g   g   animate_step("Calculating MD5 hash", 1.0)g   centered_print(f"[MD5] {md5_file(lib_path)}", C)g   g   animate_step("Exporting original library", 0.8)g   original_output = os.path.join(OUTPUT_DIR, "libapp_original.so")g   shutil.copy2(lib_path, original_output)g   centered_print("[EXPORT] Original lib exported", G)g   g   animate_step("Loading library into memory", 0.8)g   g   with open(lib_path, "rb") as file:g       data = bytearray(file.read())g   g   total_offsets = sum(len(v["offsets"]) for v in PATCHES.values())g   current = 0g   patched = 0g   g   print()g   g   for module, info in PATCHES.items():g       cyberpunk_divider()g       centered_print(f"[ MODULE ] {module}", Y)g       cyberpunk_divider()g       print()g       g       patch_bytes = hex_to_bytes(info["patch"])g       module_total = len(info["offsets"])g       module_patched = 0g       g       start_spinner("Injecting binary patch")g       g       for offset in info["offsets"]:g           current += 1g           success = patch_offset(data, offset, patch_bytes)g           progress(current, total_offsets)g           if success:g               patched += 1g               module_patched += 1g           time.sleep(ANIMATION_SPEED["patch_delay"])g       g       stop_spinner()g       print()g       centered_print("[SUCCESS] Binary patch injected successfully", G)g       centered_print(f"[INFO] {module_patched}/{module_total} memory regions modified", C)g       print()g   g   patched_output = os.path.join(OUTPUT_DIR, "libapp_patched.so")g   g   animate_step("Saving patched library", 1.2)g   start_spinner("Writing to disk")g   g   with open(patched_output, "wb") as file:g       file.write(data)g   g   stop_spinner()g   centered_print("[OK] Patched lib saved", G)g   g   animate_step("Generating patch log", 0.8)g   g   log_path = os.path.join(OUTPUT_DIR, "patch_log.txt")g   g   with open(log_path, "w") as log:g       log.write("====================================\n")g       log.write("DRAMORA PATCH LOG\n")g       log.write("====================================\n")g       log.write(f"Version      : {APP_VERSION}\n")g       log.write(f"Architecture : {ARCH}\n")g       log.write(f"Patched      : {patched}\n")g       log.write(f"Package      : {package_path}\n")g       log.write(f"Output Dir   : {OUTPUT_DIR}\n")g       log.write(f"Date         : {datetime.now()}\n")g       log.write("====================================\n")g   g   centered_print("[OK] Log saved", G)g   g   animate_step("Cleaning temporary files", 1.0)g   print()g   cyberpunk_divider()g   print()g   g   success_text = "d!PATCH COMPLETED SUCCESSFULLY d!)    print(center_text(gradient_text(success_text)))g   print()g   g   final_status = [g       centered_status_card("Patched", f"{patched} Offsets"),g       centered_status_card("Original Lib", original_output),g       centered_status_card("Patched Lib", patched_output),g       centered_status_card("Patch Log", log_path),g       centered_status_card("Temp Cleaned", "Yes"),g       centered_status_card("Status", "SUCCESS", G),g   ]g   g   for line in final_status:g       print(line)g   g   print()g   cyberpunk_divider()g   print()g   g   typing("Process completed! Press Enter to exit...", 0.005)g   input()eif __name__ == "__main__":g   main()